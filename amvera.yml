meta:
  environment: docker
  toolchain:
    name: docker
build:
  dockerfile: backend/Dockerfile.prod
  skip: false
run:
  image: null
  command: null
  args: null
  persistenceMount: /data
  containerPort: "8000"
  servicePort: "80"
